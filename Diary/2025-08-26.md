---
创建时间: 2025-08-26T17:23:43+08:00
---
## WSL2 挂载用户文件夹

既 [[2025-08-24#同 WSL 分享 SSH Agent]] 之后，`.ssh` 目录已经不需要私钥了，那 config 文件是不是也能共享掉？就像把目录挂在过来。

这里不是挂载分区磁盘的意思，我只是想把 Windows 用户目录的几个文件夹共享给 WSL2。

看到 WSL 启动后会 [自动装载设置](https://learn.microsoft.com/zh-cn/windows/wsl/wsl-config#automount-settings) 固定驱动器到 `/mnt` 下，尝试用简单的软链接共享 ssh 目录：

```
ln -s /mnt/c/Users/enihsyou/.ssh /home/enihsyou.ssh
```

本来还想能省略手动给目录挂 fstab 来着，实测一看文件权限 777，SSH 根本不认。