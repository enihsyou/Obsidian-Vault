---
创建时间: 2025-11-17T00:30:46+08:00
修改时间: 2025-11-17T17:23:29+08:00
---
## 使用 MACVLAN 为主机同时分配静态 IP 并取得动态 IP

有了之前 [[2025-11-07#节点 hostname 变更后 /etc/pve 目录清空]] 的 [[2025-11-16#PVE 目录又一次没了]] 的经验，管理端口不设置静态地址还是比较危险的。
但我还是想 [[2025-11-07#让 DNSmasq 响应 DHCPv4 主机的 IPv6 地址]]，有个好主意，让主机网卡拥有多个 IP 地址。

1. 一张网卡两个 IP
也就是 IP alias 技术，或者叫 secondary IP，挺常用的，在 [[RG-MA3063 开启 SSH#通过网络连接到设备]] 也记录过。

2. 两张网卡两个 IP
好处是可以用不同的 MAC 地址，在 DHCP 时能获得与静态不同的 IP。

```txt title="/etc/network/interfaces"
auto enp5s0
iface enp5s0 inet manual
#Qualcomm Atheros Killer E2500 Gigabit Ethernet Controller

auto vmbr0
# 这里静态地址已经在 DHCP 上注册过了，重复一遍是为了在 DHCP 上产生解析记录
iface vmbr0 inet dhcp
iface vmbr0 inet static
        address 192.168.9.17/24
        bridge-ports enp5s0
        bridge-stp off
        bridge-fd 0
# 获得 SLAAC 地址
iface vmbr0 inet6 auto

# 通过 DHCP 获得一个地址
auto mgmt
iface mgmt inet dhcp
        pre-up ip link add link vmbr0 name mgmt type macvlan mode bridge
        post-down ip link del mgmt
# 获得 SLAAC 地址
iface mgmt inet6 auto
```

## 使用 SLAAC 获得长短两个 IPv6 地址

在配置了 IPv6 Passthrough 的环境里，我发现在启用 `accept_ra` 的基础上，再设置 `inet6 auto` 可以多获得个短地址。

```txt {"取得 DHCP 地址": 2} {"取得 SLAAC 长地址": 4} {"取得 SLAAC 短地址": 6}
auto vmbr0

iface vmbr0 inet dhcp

        post-up sysctl net.ipv6.conf.vmbr0.accept_ra=2

iface vmbr0 inet6 auto
```

`accept_ra` 设置为 1 或者 2 都行，这里设置为 2 是要在开启 ip_forward 的基础上也获得 SLAAC 地址

结果是这样，一个 /128 一个 /64

```ansi {"来自 inet6 auto": 5-6} {"来自 accept_ra": 8-9}
3: [36mvmbr0: [39m<BROADCAST,MULTICAST,UP,LOWER_UP> mtu 1500 qdisc noqueue state [32mUP [39mgroup default qlen 1000
    link/ether [33m1c:1b:0d:95:90:d1[39m brd [33mff:ff:ff:ff:ff:ff[39m
    inet [35m192.168.9.17[39m/24 brd [35m192.168.9.255 [39mscope global dynamic vmbr0
       valid_lft 85372sec preferred_lft 85372sec

    inet6 [34m2409:8a1e:6940:36d0::3af[39m/128 scope global dynamic
       valid_lft 86396sec preferred_lft 86396sec

    inet6 [34m2409:8a1e:6940:36d0:1e1b:dff:fe95:90d1[39m/64 scope global dynamic mngtmpaddr proto kernel_ra
       valid_lft 257160sec preferred_lft 170760sec
    inet6 [34mfe80::1e1b:dff:fe95:90d1[39m/64 scope link proto kernel_ll
       valid_lft forever preferred_lft forever[0m
```

更可以用 VLAN 来分端口，可我手上没有支持 VLAN 的设备。

## 构建 ogimage 提示 `unsupported opentype signature <!do` 错误

在腾讯云构建 Pages 时，最近常在 ogimage.tsx 调用 satori 的部分失败，完整错误是 `` failed to emit from plugin `customogimages`: unsupported opentype signature <!do ``，之前出现过几次，但重试就好了。看结构像是 `<!doctype html>` 的一部分

```log
failed to emit from plugin `customogimages`: unsupported opentype signature <!do
    at generatesocialimage (../plugins/emitters/ogimage.tsx:52:15)
    at processogimage (../plugins/emitters/ogimage.tsx:84:18)
    at object.emit (../plugins/emitters/ogimage.tsx:120:9)
    at ../processors/emit.ts:24:28
    at async promise.all (index 10)
    at emitcontent (../processors/emit.ts:18:3)
```

问题没复现，先加个日志等等

## OpenWrt 支持注册 IPv6 解析

之前在 ASUS Merlin 固件上搞了 [[2025-11-07#让 DNSmasq 响应 DHCPv4 主机的 IPv6 地址]]，其实就是加一行 `dhcp-range=::,constructor:br0,ra-names` 配置。但在 OpenWrt 上 dnsmasq.conf 是由 `/etc/config/dhcp` 生成的，那么得有个地方添加这个属性。

找遍了也没有比较直观的地方可以进行这个配置，那么诉诸自定义配置肯定可以。好在虽然文档上没有，[Add Dnsmasq Custom Options field in LuCi GUI - For Developers - OpenWrt Forum](https://forum.openwrt.org/t/add-dnsmasq-custom-options-field-in-luci-gui/193184/18) 新增的 extraconftext 能承载任意文本。

```shell
uci set dhcp.@dnsmasq[0].extraconftext='dhcp-range=::,constructor:br0,ra-names'
uci commit dhcp
/etc/init.d/dnsmasq restart
```